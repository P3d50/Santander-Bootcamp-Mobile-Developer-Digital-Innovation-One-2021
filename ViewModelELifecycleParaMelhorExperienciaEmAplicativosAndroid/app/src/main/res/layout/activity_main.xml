<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textContador"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="30dp"
        android:layout_marginTop="30dp"
        android:layout_marginRight="30dp"
        android:text="Contador"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/editTextContador"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:ems="10"
        android:inputType="textPersonName"
        app:layout_constraintEnd_toEndOf="@+id/textContador"
        app:layout_constraintStart_toStartOf="@+id/textContador"
        app:layout_constraintTop_toBottomOf="@+id/textContador" />

    <Button
        android:id="@+id/buttonDados"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="Dados"
        app:layout_constraintEnd_toEndOf="@+id/editTextContador"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="@+id/editTextContador"
        app:layout_constraintTop_toBottomOf="@+id/editTextContador" />

    <Button
        android:id="@+id/buttonMostrar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="Mostrar"
        app:layout_constraintEnd_toEndOf="@+id/buttonDados"
        app:layout_constraintStart_toStartOf="@+id/buttonDados"
        app:layout_constraintTop_toBottomOf="@+id/buttonDados" />

</androidx.constraintlayout.widget.ConstraintLayout>